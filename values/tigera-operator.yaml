# Disable Helm-managed Tigera configs
# Configure via ArgoCD-managed YAMLs in SnoFox/k8s
installation:
  enabled: false
defaultFelixConfiguration:
  enabled: false
tolerations:
  - key: CriticalAddonsOnly
    operator: Exists
    effect: NoSchedule
