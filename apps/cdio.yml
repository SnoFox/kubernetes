apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  namespace: argocd
  name: cdio
  labels:
    snofox.net/catagory: productivity
spec:
  project: default
  destination:
    server: https://kubernetes.default.svc
    namespace: cdio
  source:
    repoURL: https://gitlab.com/SnoFox/kubernetes.git
    path: apps/cdio
    targetRevision: master
  syncPolicy:
    automated:
      enable: true
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true

