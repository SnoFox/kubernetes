apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: recipes
  namespace: tandoor
spec:
  ingressClassName: tailscale
  rules:
  - http:
      paths:
      - backend:
          service:
            name: recipes
            port:
              number: 8080
        path: /
        pathType: Prefix
  tls:
  - hosts:
    - recipes
