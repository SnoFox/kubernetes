apiVersion: v1
kind: Service
metadata:
  name: thelounge
  namespace: irc
  labels:
    app: thelounge
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 9000
    name: thelounge
  selector:
    app: thelounge
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: thelounge
  namespace: irc
spec:
  selector:
    matchLabels:
      app: thelounge
  serviceName: thelounge
  replicas: 1
  template:
    metadata:
      labels:
        app: thelounge
      annotations:
        diun.enable: "true"
    spec:
      nodeName: holo.snofox.net
      containers:
      - name: thelounge
        imagePullPolicy: Always
        image: linuxserver/thelounge:4.4.3@sha256:6ae5c9c616b981a8b0b27c441515790f3165c439579b9a985234f2f55ea8655a
        resources:
          requests:
            memory: "2Gi"
            cpu: "500m"
        ports:
        - containerPort: 9000
          name: thelounge
        env:
        - name: TZ
          value: "America/Los_Angeles"
        - name: PUID
          value: "1000"
        - name: PGID
          value: "1000"
        volumeMounts:
        - name: configdir
          mountPath: /config
  volumeClaimTemplates:
    - metadata:
        name: configdir
      spec:
        accessModes: ["ReadWriteOnce"]
        storageClassName: "freenas-iscsi-csi"
        resources:
          requests:
            storage: 100Gi
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  namespace: irc
  name: thelounge-ts
  annotations: {}
spec:
  ingressClassName: tailscale
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: thelounge
            port:
              number: 80
  tls:
  - hosts:
    - lounge
